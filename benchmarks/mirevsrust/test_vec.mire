import lists as lists

pub fn main: () {
    set arr = [] :vec![i64] mut
    set arr = lists.push(arr 1)
    set arr = lists.push(arr 2)
    set arr = lists.push(arr 3)
    
    use dasu("len {len(arr)}")
}