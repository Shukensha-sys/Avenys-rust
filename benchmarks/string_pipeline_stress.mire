import strings as strings

set i = 0 :i64 mut
set out = "" :str mut

while i < 8000 {
    set line = strings.replace(" pending-item " "-" "_") :str mut
    set line = strings.to_upper(line)
    set line = strings.trim(line)
    set out = out + line
    set i += 1
}
use dasu("out_len {len(out")})