pub fn main: () {
    set data = []  :vec![i64] mut
    set data = data + [1]
    set data = data + [2]
    set len = len(data)
    use dasu(len {len})
    use dasu(first {data at 0})
    use dasu(second {data at 1})
}