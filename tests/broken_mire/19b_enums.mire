import std

enum Result {
    Ok
    Err
}
pub fn main: () {
    set r = Result.Ok
    match r {
        Ok {
            use dasu(ok)
        }
        Err {
            use dasu(err)
        }
    }
}