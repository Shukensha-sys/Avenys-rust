load kioto

enum Result {
    Ok(value :i64)
    Err(msg :str)
}
pub fn main: () {
    set r = (Result Ok 42)
    match r {
        Ok(v) {
            use dasu("v: {v}")
        }
        Err(e) {
            use dasu(e)
        }
    }
}