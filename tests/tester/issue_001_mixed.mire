load kioto

enum Status {
    Ok
    Error
}

pub fn main: () {
    set r = Status.Error
    set m = match r {
        Status.Ok { "x" }
        Status.Error { "success" }
    } :str
    use dasu(m)
}