import std

enum Color {
    Red
    Green
    Blue
}

pub fn main: () {
    set c = Color.Green
    set result = match c {
        Color.Red { 1 }
        Color.Green { 2 }
        Color.Blue { 3 }
    } :i64
    use dasu(result)
}