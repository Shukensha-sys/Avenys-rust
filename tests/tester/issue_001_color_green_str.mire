load kioto

enum Color {
    Red
    Green
    Blue
}

pub fn main: () {
    set c = Color.Green
    set n = match c {
        Color.Red { "red" }
        Color.Green { "green" }
        Color.Blue { "blue" }
    } :str
    use dasu(n)
}