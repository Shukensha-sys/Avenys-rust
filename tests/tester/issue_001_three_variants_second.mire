load kioto

enum Status {
    First
    Second
    Third
}

pub fn main: () {
    set s = Status.Second
    set m = match s {
        Status.First { "one" }
        Status.Second { "two" }
        Status.Third { "three" }
    } :str
    use dasu(m)
}