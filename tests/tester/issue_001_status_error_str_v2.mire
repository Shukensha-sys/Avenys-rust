load kioto

enum Status {
    Ok
    Error
}

pub fn main: () {
    set r = Status.Error
    set m = match r {
        Status.Ok { "success" }
        Status.Error { "failed" }
    } :str
    use dasu(m)
}