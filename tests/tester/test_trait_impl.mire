load kioto

pub skill Printable {
    fn print: (self) :str
}

struct Person {
    name :str
}

impl Printable for Person {
    fn print: (self) :str {
        return self.name
    }
}

pub fn main: () {
    set p = (Person name: "Alice")
    set text = p.print()
    use dasu(text)
}