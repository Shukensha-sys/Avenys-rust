import std

enum Color {
    Red
    Green
    Blue
}

pub fn main: () {
    set c = Color.Blue
    set n = match c {
        Color.Red { 1 }
        Color.Green { 2 }
        Color.Blue { 3 }
    } :i64
    use dasu(n)
}