load kioto

enum Status {
    Ok
    Error
    Loading
}

pub fn main: () {
    set r = Status.Error
    set m = match r {
        Status.Ok { true }
        Status.Error { false }
        Status.Loading { false }
    } :bool
    use dasu(m)
}