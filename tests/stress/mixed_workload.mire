fn calculate: (n: i64) :i64 {
  if n <= 0 {
    return 0
  }
  set sum = 0 :i64 mut
  set i = 0 :i64 mut
  while i < 100 {
    set sum = sum + i
    set i = i + 1
  }
  return sum + calculate(n - 1)
}

set result = calculate(1000) :i64 mut

use dasu(result)