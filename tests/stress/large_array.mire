load kioto::lists

set arr = [] :vec[i64] mut
set i = 0 :i64 mut

while i < 1000 {
  set arr = lists.push(arr i)
  set i = i + 1
}

set sum = 0 :i64 mut
set i = 0 :i64 mut

while i < len(arr) {
  set sum = sum + lists.get(arr i)
  set i = i + 1
}

use dasu(sum)