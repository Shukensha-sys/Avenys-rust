// Test: multi-statement closure used as a callable variable (not thread_spawn).
load kioto

pub fn main: () {
    set base = 10 :i64
    set g = (x :i64) => {
        use dasu("got x=" + str(x))
        set y = base + x
        y * 2
    }
    set ret = g(5)
    use dasu("ret: " + str(ret))
}
