load kioto

enum Result {
    Ok(value :i64)
    Err(msg :str)
}

pub fn main: () {
    set r = Result.Ok(42)

    match r {
        Result.Ok(v) { use dasu("ok: {v}") }
        Result.Err(m) { use dasu("err: {m}") }
    }
}