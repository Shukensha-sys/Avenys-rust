import std

pub fn main: () {
    set vec = [] :vec[i64] mut
    set vec = lists.push(vec 1)
    set vec = lists.push(vec 2)
    set vec = lists.push(vec 3)
    set first = lists.get(vec 0)
    use dasu("first = {first}")
}