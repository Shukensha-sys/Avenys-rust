// Test: kioto submodule imports (restructured directory format)
load kioto::strings
load kioto::time

pub fn main: () {
    set s = strings.to_upper("test")
    use dasu(s)

    set mark = time.mark()
    use dasu(str(mark))
}
