load kioto

fn factorial: (n :i64) :i64 {
    if n <= 1 {
        return 1
    }
    return n * factorial(n - 1)
}

pub fn main: () {
    set f5 = factorial(5)
    set f10 = factorial(10)
    use dasu("5! = {f5}")
    use dasu("10! = {f10}")
}
