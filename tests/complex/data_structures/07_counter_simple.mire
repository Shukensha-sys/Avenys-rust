import std

struct Counter {
    value :i64 mut
    step :i64
}

pub fn main: () {
    set c = (Counter value: 0, step: 5) mut
    set c.value = c.value + c.step
    set v1 = c.value
    set c.value = c.value + c.step
    set v2 = c.value
    use dasu("v1={v1} v2={v2}")
}
