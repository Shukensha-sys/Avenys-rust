import std

struct Student {
    name :str
    grade :i64
}

pub fn main: () {
    set s1 = (Student name: "Alice" :str, grade: 90)
    set s2 = (Student name: "Bob" :str, grade: 85)

    use dasu("s1 created")
}