load kioto

enum Color {
    Red
    Green
    Blue
}

pub fn main: () {
    set c = Color.Red

    match c {
        Color.Red { use dasu("red") }
        Color.Green { use dasu("green") }
        Color.Blue { use dasu("blue") }
    }
}