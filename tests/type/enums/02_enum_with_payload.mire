import std

enum Maybe {
    None
    Some(value :i64)
}

pub fn main: () {
    set m = Maybe.Some(value: 42)

    match m {
        Maybe.None { use dasu("nothing") }
        Maybe.Some(v) { use dasu("value = {v}") }
    }
}