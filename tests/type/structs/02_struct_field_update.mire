load kioto

struct User {
    name :str
    age :i64
}

pub fn main: () {
    set user = (User name: "Alice" age: 30)
    set name_upper = strings.to_upper(user.name)
    use dasu("name={name_upper} age={user.age}")
}