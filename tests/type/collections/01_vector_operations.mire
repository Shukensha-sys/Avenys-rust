load kioto

pub fn main: () {
    set arr = [] :vec[i64] mut
    set arr = lists.push(arr 10)
    set arr = lists.push(arr 20)
    set arr = lists.push(arr 30)
    set len = len(arr)
    use dasu("length = {len}")
}