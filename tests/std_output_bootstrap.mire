import lists as lists

pub fn main: () {
set user = "mire"
set pending = [] :vec[i64] mut
set pending = lists.push(pending 2)
set pending = lists.push(pending 4)

use dasu("Boot ok {user}")
use dasu("Pending {len(pending)}")
}
