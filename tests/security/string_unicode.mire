load kioto

pub fn main: () {
    set text = "Hola Mundo"
    set upper = strings.to_upper(text)
    set lower = strings.to_lower(text)
    set trimmed = strings.trim(text)
    use dasu("len {len(text)}")
    use dasu(upper)
    use dasu(lower)
}